import Ember from 'ember';

export default Ember.Route.extend({
  /*
  model: function(userID){
    //return a promise
    //return $.ajax('users/' + userID, { })
    //below is the detault code generated on the fly by ember
    return this.store.find('user', userID)
  }
  */
});
